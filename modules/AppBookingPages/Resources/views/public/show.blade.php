@php($services = \Modules\AppBookingPages\Models\BookingService::query()->where('business_id', $campaign->business_id)->where('is_active', true)->get())
<!doctype html><html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $campaign->name }}</title><style>body{margin:0;font-family:Inter,ui-sans-serif,system-ui;background:#f7f8fb;color:#172033}.wrap{min-height:100vh;display:grid;place-items:center;padding:24px}.panel{width:min(560px,100%);border:1px solid #dfe5ef;background:#fff;border-radius:18px;padding:28px;box-shadow:0 24px 80px rgba(15,23,42,.08)}.field{width:100%;box-sizing:border-box;border:1px solid #dfe5ef;border-radius:12px;padding:12px;margin-top:10px}.submit{border:0;border-radius:12px;background:#2563eb;color:white;padding:12px 16px;font-weight:700;margin-top:12px}</style></head><body><main class="wrap"><section class="panel"><p>{{ $campaign->business?->name }}</p><h1>{{ __('Book an appointment') }}</h1>@if(session('booking_saved'))<p>{{ __('Your booking request was sent.') }}</p>@else<form method="post" action="{{ route('booking-pages.submit', $campaign) }}">@csrf<select class="field" name="service_id">@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->name }} @if($service->price) - {{ format_price_locale((float) $service->price) }} @endif</option>@endforeach</select><input class="field" type="date" name="booking_date" required><input class="field" type="time" name="booking_time" required><input class="field" name="customer_name" placeholder="{{ __('Your name') }}" required><input class="field" name="customer_phone" placeholder="{{ __('Phone') }}"><input class="field" name="customer_email" placeholder="{{ __('Email') }}"><textarea class="field" name="note" rows="3" placeholder="{{ __('Note') }}"></textarea><button class="submit" type="submit">{{ __('Confirm booking') }}</button></form>@endif</section></main></body></html>
